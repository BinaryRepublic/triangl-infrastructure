apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: dashboard
spec:
  template:
    metadata:
      labels:
        app: dashboard
    spec:
      containers:
      - name: dashboard
        image: eu.gcr.io/triangl-215714/dashboard:latest
